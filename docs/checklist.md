# CHECKLIST

- [x] `middleware.ts` 내 `httpOnly: false` 설정 적용
- [x] `auth/callback/route.ts` 내 `httpOnly: false` 설정 적용
- [x] `middleware.ts` 리다이렉트 응답의 쿠키 복사 로직 안정화
- [x] `user-store.ts` 초기화 로직 수정 (HMR/상태 리셋 대응)
- [x] API 프로필/장바구니 조회 타임아웃 추가
